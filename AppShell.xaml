<?xml version="1.0" encoding="UTF-8"?>

<Shell
    x:Class="JL_CW_App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:xaml="http://schemas.mauimicromvvm.com/2022/dotnet/maui"
    xmlns:viewModels="clr-namespace:JL_CW_App.ViewModels"
    xmlns:views="clr-namespace:JL_CW_App.Views"
    BackgroundColor="{DynamicResource Primary}"
    Shell.TabBarBackgroundColor="{DynamicResource Primary}"
    Shell.TabBarTitleColor="White"
    Shell.TitleColor="White"
    Shell.FlyoutBehavior="Flyout"
    Shell.TabBarUnselectedColor="Azure"
    xaml:MauiMicro.Autowire="True"
    x:DataType="viewModels:AppShellViewModel"
    Title="Budget App">
    
    <Shell.FlyoutFooter>
        <Button WidthRequest="200" Margin="0,0,0,30" Text="Logout" BackgroundColor="Red" Command="{Binding LogOutCommand}" />
    </Shell.FlyoutFooter>
    
    <ShellContent
        Title="Login"
        FlyoutItemIsVisible="False"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Route="login" />

    <Tab Title="ArticlesPage">
        <ShellContent
            ContentTemplate="{DataTemplate views:ArticlesPage}"
            Route="ArticlesPage" />
    </Tab>
    <Tab Title="BmiPage">
        <ShellContent
            ContentTemplate="{DataTemplate views:BmiPage}"
            Route="BmiPage" />
    </Tab>
    <Tab Title="Quote Generator">
        <ShellContent
            ContentTemplate="{DataTemplate views:QuoteGeneratorPage}"
            Route="QuoteGeneratorPage" />
    </Tab>
    <Tab Title="SavedQuotesPage">
        <ShellContent
            ContentTemplate="{DataTemplate views:SavedQuotes}"
            Route="SavedQuotesPage" />
    </Tab>
</Shell>